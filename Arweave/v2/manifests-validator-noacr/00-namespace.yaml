# ============================================================
# 00-namespace.yaml
# Creates the dedicated Kubernetes namespace for arweave-node
# Apply this first, before any other manifest
# ============================================================


apiVersion: v1
kind: Namespace
metadata:
  name: arweave
  labels:
    app: arweave-node
    managed-by: manifest-generator
    environment: non-prod
